export enum num {
  ONE_THOUSAND = 1000,
  ONE_MILLION = 1000000,
  POSITIVE = 1,
  NEGATIVE = -1,
}

export enum shiftDate {
  FIRST_DATE_DAYS = 7,
  SECOND_DATE_MONTH = 1,
  THIRD_DATE_MONTH = 6,
}

export enum sortDirection {
  DESK = 'desk',
  ASK = 'asc'
}

export enum queryValues {
  type = 'video',
  part = 'snippet',
  partStats = 'snippet,statistics',
  maxResults = '16',
}
